name: "2. Multi OS Check"

on:
  push:
    branches: [ ANDERSON ]
  pull_request:
    branches: [ ANDERSON ]

jobs:
  check-os:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check OS info
        run: |
          echo "Sistema operativo: ${{ matrix.os }}"
          uname -a
